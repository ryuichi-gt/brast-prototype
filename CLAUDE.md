# BRAST — Brand Strategy OS / プロジェクト指示書 (CLAUDE.md) ※v2

> セッション開始時に必ず読む。**確定仕様はここ。** 詳細は `reference/`。
> v2 の変更点: 「メディア生成器」から本来の **Brand Strategy OS** へ重心を戻す。
> 3本柱（ブランド運用ルール／グラスボックス推論／閉じたループ）を中核に据え直した。

---

## 0. リファレンスと優先順位

1. **CLAUDE.md（本書）** = 確定事項・全体方針。
2. **`reference/strategy-logic.md`** = 戦略導出ロジックの正（研究→戦略のアルゴリズム）。推論グラフのノード定義でもある。
3. **`reference/ux-flow.md`** = 全体UXフローと画面仕様の正。
4. **`reference/brast-mockup.html`** = 既存UIの正（M1〜M4の見た目・既存画面）。
5. **`reference/meeting-transcript.md` / `meeting-transcript-2.md`** = 意図の原典。迷ったときだけ参照。自動文字起こしでノイズが多いので鵜呑みにしない。

実装の判断: 戦略の中身は strategy-logic を、画面は ux-flow を正とする。既存の見た目は mockup を踏襲。迷ったら transcript。

---

## 1. プロダクトの最終形態 ＝ 3本柱（最重要）

BRAST は「スライドを作る道具」ではなく、**ブランドの憲法に統治された自律ブランドストラテジスト**。

1. **ブランド運用ルール（ガバナンス層）★新** ― ブランドの憲法。これに反する戦略・コンテンツ・*外部からの修正指示*は跳ね返す。これが無いと「何でもあり」になり、出力が正しいと言える基準が無い。
2. **グラスボックス（推論が辿れる）** ― 根拠→結論まで全部辿れる。スライドは氷山の一角、価値はその下の推論。
3. **閉じたループ** ― 提案→修正→承認→実行→結果計測→翌週反映 を行き来できる。

> 既存ビルド(M1〜M4)は柱2の「出力(スライド)」だけ。**柱1が無く、柱2の推論と柱3のループがスタブ。** v2 でこれを立てる。

---

## 2. 改訂パイプライン

```
① ナレッジベース構築
② ブランド運用ルール定義 ★新（AI提案→人間承認・改訂可。出面戦略＋ガードレール＋KPI）
③ トレンド調査 + 競合調査（概念理解で関連度フィルタ・多ソース）
④ 戦略導出（S1〜S6 = strategy-logic.md。推論グラフとして可視化）
⑤ メディアプラン → コンテンツ生成 → 画像生成 → 多段レビュー(4体) → スコアゲート(90)
⑥ 人間レビュー → 配信
⑦ 実行 → 結果計測 → 翌週へ反映 ★新（ループを閉じる）
```
★が現プロトタイプに無い/スタブ。④は出力だけ実装済みなので**中身（推論）を可視化**する。

---

## 3. 既存実装(M1〜M4)＝触ってよい土台

Next.js(App Router)+TS+Tailwind。共通シェル/7スライドのブリーフィング/思考プロセス(=ステータスバー)/フィードバックdock/6つの薄い副画面/テナント切替/スコア90。fetch層は `src/lib/api.ts` に分離済み。デザイントークンは globals.css。**これを土台に拡張する（作り直さない）。**

---

## 4. v2で追加・改修するもの（→ build-brief.md の M5〜M9）

- **柱1:** ブランド運用ルール画面＋AI提案フロー（②）。
- **柱2:** 「思考プロセス(ステータスバー)」→「推論グラフ(S1〜S6・ドリルダウン)」へ格上げ。スライド主張→根拠への逆リンク。
- **修正点②:** コンテンツ詳細＋インライン修正チャット＋ブランド押し返し。
- **柱3:** 実行・結果画面、戦略アーカイブの結果表示、ナレッジ/トレンドを「根拠として辿れる」配線。
- **品質:** 関連度フィルタの理由表示（BCPで無関連を拾う問題の解決）、画像生成は Phase 2。

---

## 5. エージェント構成（変更なし）

Research / Strategy Orchestrator / Content / Visual / Review Cluster(4体: AIライティング検知・法務景表法・レピュテーション・IR専門) / Score Gate(90) / Human Review / Distribution。構成は閲覧のみ・編集不可。

---

## 6. 開発フロー & 規約

- 環境: Claude Code(web/cloud) → GitHub PR → Vercel プレビュー。
- **1マイルストーンずつ。各完了でPR更新＋簡潔に報告。**（巨大な一括実装をしない＝品質・コンテキスト保全のため）
- 確定できない仕様はダミー＋「要確認」で進めて止めない。
- コード/コメントは英語。teal=AI・amber=人間。localStorage不使用。データ取得はfetch層分離を維持。
- スコアしきい値=90（確定）。tenant>channel。

---

## 7. 確定事項

- スコアしきい値 **90/100**（2026-06-21 岡本さん確定）。
- 製品の重心 = 3本柱（v2でガバナンス層を新設）。
- 既存M1〜M4は土台として維持・拡張。
