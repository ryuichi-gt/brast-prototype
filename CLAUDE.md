# BRAST — Brand Strategy OS / プロジェクト指示書 (CLAUDE.md)

> Claude Code はセッション開始時に必ずこのファイルを読むこと。
> **確定仕様はここ。** 詳細な根拠・ニュアンスは `reference/` を参照。

---

## 0. リファレンス資料と優先順位（重要）

実装の判断は、次の優先順位に従う。

1. **`CLAUDE.md`（このファイル）** = 確定事項。最優先。
2. **`reference/brast-mockup.html`** = UI の正（source of truth）。**人間レビュー済み**の画面仕様。配色・タイポ・レイアウト・画面遷移・インタラクションはこれを踏襲する。
3. **`reference/meeting-transcript.md`** = 意図の原典。**判断に迷ったとき／背景の意図を確認したいときだけ参照**する。

**ルール:**
- 実装は `CLAUDE.md` と `mockup` を正として進める。
- transcript は **自動文字起こし＋機械翻訳のため、ノイズが多い**。具体的には:
  - 機械翻訳の崩れ（例:「サッケーション」= suggestion、「冗長」= 上長/役員 の誤変換など）
  - 字幕アーティファクト（例:「ご視聴ありがとうございました」が無関係に混入）
  - 話者 Long Trần は非ネイティブ日本語で、発話が一部崩れている
  - 末尾の「電気工事業者」は翻訳バグの雑談で、**プロダクトと無関係**
- transcript を実装仕様として鵜呑みにしない。確定事項は本ファイルにまとめてある。

---

## 1. プロダクト概要

**BRAST = Brand Strategy OS**（旧称 BrandOS / MediaOps）。
エンタープライズ向けの **AI 自律メディア運用 OS**。社内では「AI CMO / Marketing OS」とも。

シンパフィット等のように「中身は研究・専門性が本質でブランディングが得意でない会社」が、外部代行に多額を払わずとも、正しい価値を継続的に world に発信できるようにする。中小・マス向けの「記事生成補助ツール」ではない。

---

## 2. コア UX 思想（最重要・絶対に外さない）

- ❌ タブを切り替えて「ナレッジ → DNA → コンテンツ」と人間が手で操作する道具 **ではない**。
- ✅ **「自律的にメディア運営をしてくれる、超優秀な部下」** が、毎週**フルオートで戦略を作り切り**、**スライド + 説明テキスト**で人間（上司）に**プレゼンしてくる**プロダクト。
- 人間は上司として **レビュー → 承認 or 差し戻し** をするだけ。
- 差し戻しは **「気に入らない結論を生んだ原因のエージェント工程まで遡り、その下流だけ再実行」**。既に承認済みの成果物は残す。
- 主たる操作画面 = **週次ブリーフィング（戦略のプレゼン）**。
- **チャットボットは一機能にすぎない。チャットボットに UX を寄せない**（前回の試作はここが逆だった）。
- レビュー周期は **週次（ウィークリー）**。人間はスライドを見ることに慣れているため、出力はスライド + 説明テキスト。
- フィードバックは「部下に指示する」感覚で出せること（自然文 or 各スライドへのコメント）。

---

## 3. 確定した思考順序（パイプライン）

```
ベースナレッジ
  → リアルタイム・トレンド/競合調査
    → 戦略統合
      → メディアプラン
        → コンテンツ生成
          → 画像生成
            → 多段レビュー(4体・並列)
              → スコアゲート
                → 人間レビュー(YOU)
                  → 配信
```

- **コンテンツ生成は戦略の下流。戦略こそが肝。この順序を絶対に逆にしない。**
- ベースナレッジは「会社が意図的に更新しない限り変わらない前提情報」。差分検知だけ行い、勝手に書き換えない。
- トレンド調査は **自動・定期実行**（例: 毎週月曜 06:00）。この結果が戦略の根拠になる。

---

## 4. エージェント構成（オーケストレーション）

Agent SDK でソースコード側がオーケストレーションする（Phase 1 ではダミーで表現）。
ユーザーはエージェントの**中身を閲覧できる**が、**編集はできない**（用意された構成を選んで使う設計）。

| エージェント | 役割 |
|---|---|
| **Research（トレンド調査）** | ナレッジを前提に、制度・市場・SNS・競合をリアルタイム収集。関連度でスコア化。 |
| **Strategy Orchestrator（戦略統合）** | 市場・競合エージェントの結論を統合し、定常発信＋キャンペーンの2トラックで週次戦略を決定。 |
| **Content（コンテンツ生成）** | 戦略に紐づけ、媒体別に記事・SNS草稿・配布資料を生成。 |
| **Visual（画像生成）** | 各記事のキービジュアルを生成。ブランドのビジュアルトーン準拠。 |
| **Review Cluster（多段レビュー・4体並列・ゲート）** | ①AIライティング検知 ②法務・景表法 ③レピュテーション ④IR・専門。1つでも未達なら生成へ差し戻し。 |
| **Score Gate（スコアゲート）** | しきい値 **90 / 100**。超→自動承認候補、未満→人間レビュー推奨。 |
| **Human Review（YOU）** | 承認 or 該当工程へ差し戻し。 |
| **Distribution（配信）** | 自動連携（API）と手動投稿（指示のみ）を出し分け。 |

> ✅ **確定（2026-06-21 岡本さん）:** スコアしきい値は **90 / 100**（transcript の発言準拠）。mockup は 95 だったが 90 で確定。実装では `src/data/config.ts` の `SCORE_THRESHOLD` に集約。

---

## 5. 2トラック（定常 / キャンペーン）

- **定常発信**: 目的＝認知・信頼の積み上げ。特定キャンペーンなし、継続的なブランド発信。
- **キャンペーン**: 期間付き（1ヶ月〜半年）。目的＝CV・指名流入。期間中にトレンドが変化したら、**未来の記事を再評価・更新**する（既に配信済みの採用記事もスキャンしてアップデート）。

AI は週次ブリーフィングで、この2つを**別々の提案**として人間に提示する。

---

## 6. 情報設計（IA / 画面）

**ナビゲーション:** 週次ブリーフィング / 戦略アーカイブ / トレンド・レーダー / ブランドナレッジ / コンテンツ / チャネル / エージェント

**週次ブリーフィングのスライド構成（7枚・この順）:**

1. **エグゼクティブサマリー** — 今週のテーゼ + KPI + 2トラック概要
2. **ナレッジ状態** — 差分検知（fresh / 更新候補）
3. **マーケットトレンド** — ライブ信号 + 関連度スコア
4. **競合の動き** — 競合の発信、空白テーマ
5. **戦略** — 「なぜこの戦略か」: 各エージェントの結論 → 統合判断
6. **今週のメディアプラン** — 記事ごとにチャネル/スコア/旧版vs新版
7. **配信スケジュール** — 週カレンダー（自動/手動を区別）

- 各スライドは**差し戻し可**。差し戻すと右の思考プロセス（パイプライン）で該当工程がハイライトされ、**下流だけ再実行**できる。
- フィードバック dock で自然文の指示も受ける（意図に応じて該当工程まで遡る）。

**ダッシュボード（戦略アーカイブ）:** 週次戦略シートが積み上がる（実行済み / 予定）。
**コンテンツ:** 配信予定 / 配信済み。戦略変更時は **旧版 vs 新版を並列提示**し、人間が選択。
**チャネル:** 複数メディアを一元管理。各チャネルの**役割を可視化**。自動連携（API）と手動投稿を区別。
**エージェント:** 構成は閲覧のみ。設定の中身は見れる（編集不可）。

---

## 7. テナント / チャネル構造

`tenant > channel` の2階層。

- **SympaFit**（main / athlete / femtech）
- **JRC エンジニアリング — 医療ITソリューション事業部**（オウンドメディア / X / LinkedIn）
- **ツズリー / BCP**（オウンドメディア / X）
- **Libero**（オウンドメディア / LinkedIn）

mockup のデフォルトテナント = **JRC 医療IT**（議事録の具体シナリオが最も濃いため）。

---

## 8. プロダクト・ポジショニング

- **エンタープライズ特化**。マス向けの記事生成補助ツール（ChatGPT の延長）には**しない**。
- PR 活動は最小、**リファラルでオーガニックに**導入を伸ばす。
- 価格イメージ: 月額 7〜8万円・使い放題（暫定・久保さん案）。
- 競合に対しては「**類似商品があった上で星取り表で選ばれる**」立ち位置（唯一無二性より比較で勝つ）。
- 複数メディアの**一元管理 × 役割の可視化 × トレンド追従のリコメンド**が差別化点。WordPress / 単一サイト CMS とは別物。
- 顧客の思考プロセス・フィードバック・抵抗が**癖として蓄積**し、乗り換え不可能なコンテキストロックを作る（＝勝ち筋）。

---

## 9. 技術スタック

**Phase 1（＝今・このリポジトリ）= フロントエンドのプロトタイプ:**
- **Next.js (App Router) + TypeScript + Tailwind CSS**
- データは**ダミー JSON**（例: `src/data/*.ts`）。バックエンドにはまだ接続しない。
- **Vercel プレビューデプロイ**前提。
- データ取得は**fetch 層を明確に分離**しておく（後でバックエンドに差し替え可能に）。

**Phase 2（DIGITRAN 担当）= バックエンド/実装:**
- FastAPI + MySQL + Celery + Redis、Agent SDK オーケストレーション、SNS OAuth フロー
- マルチAI 品質ループ（Claude / GPT-4o / Gemini, スコアしきい値）

ドメイン構成（参考）: `mediaops.internnect.ai`（admin）、`{tenant}.mediaops.internnect.ai`（テナント）。

---

## 10. 開発フロー

- **環境:** Claude Code (web / cloud) → GitHub Mobile で PR レビュー → Vercel プレビュー。
- **1画面ずつ**実装する。各ステップ完了ごとに**簡潔に報告**（act & report。途中で過度に質問しない＝自律実行を優先）。
- 確定できない仕様は実装を止めず、**ダミーで進めて「要確認」と報告**する。
- DIGITRAN への引き継ぎは **~60%（UI が固まった時点）**。それ以降はバックエンド・実データ・デプロイを DIGITRAN が担当。

---

## 11. コーディング規約

- **コード・コメントは英語**で書く（DIGITRAN 引き継ぎのため）。
- コンポーネントは画面 / ドメイン単位で分割。
- デザイントークン（色・タイポ・余白）は `globals.css` 等に集約し、**mockup の配色・タイポを踏襲**。
- **色の意味付けを厳守:** `teal` = AI の自律作業 / ライブ、`amber` = 人間の判断が必要な箇所。
- `localStorage` / `sessionStorage` は使わない（状態は React state で管理）。
- 品質フロア: キーボードフォーカス可視、`prefers-reduced-motion` 尊重、モバイル対応（崩れない）。

---

## 12. ビルド・ロードマップ（Phase 1）

具体的な貼り付け用プロンプトは **`reference/build-brief.md`** にマイルストーン別でまとめてある。

- **M1:** Next.js 雛形 + デザイントークン + 週次ブリーフィング画面（ダミーデータ）
- **M2:** 差し戻し → トレースバック → 下流再実行のインタラクション
- **M3:** 残りの画面（戦略アーカイブ / トレンド / ナレッジ / コンテンツ / チャネル / エージェント）
- **M4:** テナント・チャネル切り替え、レスポンシブ仕上げ
- → ~60% で DIGITRAN へ引き継ぎ。
